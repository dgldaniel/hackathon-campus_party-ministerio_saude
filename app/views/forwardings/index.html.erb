<p id="notice"><%= notice %></p>

<h1>Forwardings</h1>

<table>
  <thead>
    <tr>
      <th>Especialidade</th>
      <th>Hipotesediagnosticocid10</th>
      <th>Classificacaorisco</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @forwardings.each do |forwarding| %>
      <tr>
        <td><%= forwarding.especialidade %></td>
        <td><%= forwarding.hipoteseDiagnosticoCid10 %></td>
        <td><%= forwarding.classificacaoRisco %></td>
        <td><%= link_to 'Show', forwarding %></td>
        <td><%= link_to 'Edit', edit_forwarding_path(forwarding) %></td>
        <td><%= link_to 'Destroy', forwarding, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Forwarding', new_forwarding_path %>
