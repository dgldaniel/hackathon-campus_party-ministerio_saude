<%= form_with(model: exam) do |form| %>
  <% if exam.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(exam.errors.count, "error") %> prohibited this exam from being saved:</h2>

      <ul>
        <% exam.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :codigoExame %>
    <%= form.text_field :codigoExame %>
  </div>

  <div class="field">
    <%= form.label :solicitadoAvaliado %>
    <%= form.text_field :solicitadoAvaliado %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
