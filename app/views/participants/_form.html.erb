<%= form_with(model: participant) do |form| %>
  <% if participant.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(participant.errors.count, "error") %> prohibited this participant from being saved:</h2>

      <ul>
        <% participant.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :cnsParticipante %>
    <%= form.text_field :cnsParticipante %>
  </div>

  <div class="field">
    <%= form.label :dataNascimento %>
    <%= form.datetime_select :dataNascimento %>
  </div>

  <div class="field">
    <%= form.label :avaliacaoAlterada %>
    <%= form.check_box :avaliacaoAlterada %>
  </div>

  <div class="field">
    <%= form.label :cessouHabitoFumar %>
    <%= form.check_box :cessouHabitoFumar %>
  </div>

  <div class="field">
    <%= form.label :abandonouGrupo %>
    <%= form.check_box :abandonouGrupo %>
  </div>

  <div class="field">
    <%= form.label :sexo %>
    <%= form.number_field :sexo %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
