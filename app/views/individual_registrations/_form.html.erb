<div class='card-body'>
  <%= form_with(model: individual_registration, id: 'regForm', class: 'form-wizard') do |form| %>
    <% if individual_registration.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(individual_registration.errors.count, "error") %> prohibited this individual_registration from being saved:</h2>
        <ul>
          <% individual_registration.errors.each do |error| %>
            <li><%= error.full_message %></li>
          <% end %>
        </ul>
      </div>
    <% end %>
    <%= render 'user_identification', form: form %>
    <%= render 'health_condition', form: form %>
    <%= render 'sociodemographic_information', form: form %>
    <%= render 'understading', form: form %>
    <%= render 'citizen_exit_registration', form: form %>
    <%= render 'uuid_data', form: form %>
    <div>
      <div class="text-end btn-mb">
        <button class="btn btn-warning" id="prevBtn" type="button">Anterior</button>
        <button class="btn btn-primary" id="nextBtn" type="button">Próximo</button>
        <%= form.submit "Registrar Cadastro Individual", class: "btn btn-success", id: 'submitBtn', style: "display:none" %>
      </div>
    </div>
    <!-- Circles which indicates the steps of the form:-->
    <div class="text-center">
      <span class="step"></span>
      <span class="step"></span>
      <span class="step"></span>
      <span class="step"></span>
      <span class="step"></span>
      <span class="step"></span>
    </div>
    <!-- Circles which indicates the steps of the form:-->
  <% end %>
</div>
